# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: temurin
        cache: maven
    - name: Checkstyle
      run: mvn checkstyle:check -f pom.xml
    - name: SpotBugs
      run: mvn spotbugs:check -f pom.xml
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    steps:
    - uses: actions/checkout@v4
    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: temurin
        cache: maven
    - name: Unit tests
      run: mvn test -f pom.xml
    - name: Integration tests
      run: mvn verify -Pfailsafe -f pom.xml
  security:
    runs-on: ubuntu-latest
    needs:
    - lint
    steps:
    - uses: actions/checkout@v4
    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: temurin
        cache: maven
    - name: OWASP dependency check
      run: mvn org.owasp:dependency-check-maven:check -f pom.xml
  build_push:
    runs-on: ubuntu-latest
    needs:
    - test
    - security
    steps:
    - uses: actions/checkout@v4
    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: temurin
        cache: maven
    - name: Build JAR
      run: mvn package -DskipTests -f pom.xml
    - name: Log in to GHCR
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ghcr.io/${{ github.repository }}:${{ github.sha }},ghcr.io/${{ github.repository
          }}:latest
  provision:
    runs-on: ubuntu-latest
    needs:
    - build_push
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_jwt_secret: ${{ secrets.JWT_SECRET }}
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - name: Terraform apply
      run: terraform apply -input=false -auto-approve
      working-directory: infra
  puppet-bootstrap:
    runs-on: ubuntu-latest
    needs:
    - provision
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_jwt_secret: ${{ secrets.JWT_SECRET }}
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    - id: get_ip
      name: Get instance IP
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"ip=$(terraform output -raw instance_public_ip)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Write SSH key
      run: 'mkdir -p ~/.ssh

        printf ''%s\n'' "$SSH_KEY" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        ssh-keyscan -H "$HOST" >> ~/.ssh/known_hosts

        '
      env:
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        HOST: ${{ steps.get_ip.outputs.ip }}
    - name: Wait for SSH
      run: "for i in $(seq 1 20); do\n  ssh -i ~/.ssh/deploy_key -o ConnectTimeout=5\
        \ -o BatchMode=yes \\\n    \"$DEPLOY_USER@$HOST\" echo \"ready\" && break\n\
        \  echo \"Attempt $i: SSH not ready, retrying in 15s...\"\n  sleep 15\ndone\n"
      env:
        HOST: ${{ steps.get_ip.outputs.ip }}
        DEPLOY_USER: ${{ secrets.SSH_USER }}
    - name: Copy Puppet manifests
      run: scp -i ~/.ssh/deploy_key -r puppet/ "$DEPLOY_USER@$HOST:/tmp/puppet-manifests"
      env:
        HOST: ${{ steps.get_ip.outputs.ip }}
        DEPLOY_USER: ${{ secrets.SSH_USER }}
    - name: Run Puppet bootstrap
      run: "ssh -i ~/.ssh/deploy_key \"$DEPLOY_USER@$HOST\" \\\n  \"sudo DEPLOY_USER=$DEPLOY_USER\
        \ bash /tmp/puppet-manifests/bootstrap.sh\"\n"
      env:
        HOST: ${{ steps.get_ip.outputs.ip }}
        DEPLOY_USER: ${{ secrets.SSH_USER }}
  configure:
    runs-on: ubuntu-latest
    needs:
    - puppet-bootstrap
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_jwt_secret: ${{ secrets.JWT_SECRET }}
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    - id: get_ip
      name: Get instance IP
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"ip=$(terraform output -raw instance_public_ip)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Write SSH key
      run: 'mkdir -p ~/.ssh

        printf ''%s\n'' "$SSH_KEY" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        ssh-keyscan -H "$HOST" >> ~/.ssh/known_hosts

        '
      env:
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        HOST: ${{ steps.get_ip.outputs.ip }}
    - name: Install Ansible
      run: 'pipx install ansible-core

        ansible --version

        '
    - name: Run Ansible playbook
      run: "ansible-playbook playbook.yml \\\n  -i \"$HOST,\" \\\n  --private-key\
        \ ~/.ssh/deploy_key \\\n  -u \"$DEPLOY_USER\" \\\n  -e \"db_password=$DB_PASSWORD\"\
        \ \\\n  -e \"jwt_secret=$JWT_SECRET\" \\\n  -e \"admin_username=admin\" \\\
        \n  -e \"admin_password=$ADMIN_PASSWORD\" \\\n  -e \"image_tag=$IMAGE_TAG\"\
        \ \\\n  -e \"ghcr_user=$GHCR_USER\" \\\n  -e \"ghcr_token=$GHCR_TOKEN\" \\\
        \n  -e \"repo=$REPO\"\n"
      working-directory: ansible
      env:
        HOST: ${{ steps.get_ip.outputs.ip }}
        DEPLOY_USER: ${{ secrets.SSH_USER }}
        DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        JWT_SECRET: ${{ secrets.JWT_SECRET }}
        ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
        IMAGE_TAG: ${{ github.sha }}
        GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GHCR_USER: ${{ github.actor }}
        REPO: ${{ github.repository }}
  verify:
    runs-on: ubuntu-latest
    needs:
    - configure
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_jwt_secret: ${{ secrets.JWT_SECRET }}
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    - id: get_ip
      name: Get instance IP
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"ip=$(terraform output -raw instance_public_ip)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Health check
      run: "curl --fail --retry 12 --retry-delay 15 --retry-all-errors \\\n  \"http://$HOST/actuator/health\"\
        \n"
      env:
        HOST: ${{ steps.get_ip.outputs.ip }}
    - name: Verify OpenAPI document is served
      run: "curl --fail --retry 6 --retry-delay 10 --retry-all-errors \\\n  \"http://$HOST/v3/api-docs\"\
        \ -o openapi.json\necho \"OpenAPI document retrieved ($(wc -c < openapi.json)\
        \ bytes)\"\n"
      env:
        HOST: ${{ steps.get_ip.outputs.ip }}
    - name: Upload OpenAPI document
      uses: actions/upload-artifact@v4
      with:
        name: openapi-spec
        path: openapi.json
  perf:
    runs-on: ubuntu-latest
    needs:
    - verify
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_jwt_secret: ${{ secrets.JWT_SECRET }}
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    - id: get_ip
      name: Get instance IP
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"ip=$(terraform output -raw instance_public_ip)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Install k6
      run: 'curl -fsSL https://github.com/grafana/k6/releases/download/v0.54.0/k6-v0.54.0-linux-amd64.tar.gz
        -o k6.tar.gz

        tar xzf k6.tar.gz

        sudo mv k6-v0.54.0-linux-amd64/k6 /usr/local/bin/k6

        '
    - name: Run k6 performance tests
      run: k6 run tests/k6/performance.js
      env:
        BASE_URL: http://${{ steps.get_ip.outputs.ip }}
        ADMIN_USERNAME: admin
        ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
    - name: Upload k6 summary
      uses: actions/upload-artifact@v4
      with:
        name: k6-performance-summary
        path: k6-summary.json
