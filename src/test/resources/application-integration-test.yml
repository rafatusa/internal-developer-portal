spring:
  datasource:
    url: jdbc:h2:mem:idptest;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DATABASE_TO_UPPER=FALSE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: false
    url: jdbc:h2:mem:idptest;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DATABASE_TO_UPPER=FALSE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    user: sa
    password: ""

  h2:
    console:
      enabled: false

server:
  port: 0

spring.main.banner-mode: "off"

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

app:
  jwt:
    secret: integrationtestjwtsigningsecretforhs512algorithmmustbeatleast64byteslongabcdefgh
    expiration-ms: 3600000
    refresh-expiration-ms: 7200000
  security:
    bcrypt-strength: 4

logging:
  level:
    root: ERROR
    com.enterprise.idp: WARN
    org.flywaydb: ERROR
    org.springframework: ERROR
    org.hibernate: ERROR
